PORT=5000
FRONTEND_ORIGIN=http://localhost:5173
# Optional for local dev if frontend/.env.local already contains VITE_SUPABASE_URL
# and VITE_SUPABASE_ANON_KEY. Set these explicitly in deployment.
SUPABASE_URL=
SUPABASE_ANON_KEY=
